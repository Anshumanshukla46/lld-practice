**Beginner LLD concepts**

lld-practice/
│
├── README.md
│
├── q1_simpleLibraryManagment/
    ├── class-diagram.md
    ├── src/


Recommendation:
- There is some possibility that when you clone this repository it might open as plain text
  - instead of actual .java package structure
- Just follow this (if using intelliJ/eclipse):
  - Don't get permission to intellij to select for JDK
  - instead download java jdk for now can be any say java SE 17 and set that up
  - then change sdk in file > project structure > select that oracle one which was setup before
  - now open this...